package pgc.compute;


public class Main {

	public static void main(String[] args) {

		/*Variable[] var = new Variable[3];
		var[0] = new Variable("x");
		var[1] = new Variable("y");
		var[2] = new Variable("z");
		String str = "3*x^3*y*z-3*x^2+5*x*y-1-x";
		String str2 = "2*x^2+4*x*y*z-y+2";
		Polynomial p1 = new Polynomial(str, var);
		Polynomial p2 = new Polynomial(str2, var);
		Polynomial de= GeneratingFunctions.dedup(p1);
		System.out.print("" + de.toString());
		Polynomial trunc = GeneratingFunctions.trunc(p1, var[0], 2);
		System.out.print("" + trunc.toString());
		Polynomial minf = GeneratingFunctions.minF(p1, p2);
		System.out.print("" + minf.toString());*/
	}
}
